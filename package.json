{
    "type": "module",
    "scripts": {
        "build": "rollup -c"
    },
    "devDependencies": {
        "@rollup/plugin-inject": "^5.0.5",
        "@wasm-tool/rollup-plugin-rust": "^2.4.5",
        "rollup": "^4.14.1"
    }
}
